<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".ContentView"></activity>
        <activity
            android:name=".splashScreenA"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            tools:ignore="ExtraText">
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".Login"></activity>
        <activity android:name=".Teacher.AddContent" />
        <activity android:name=".meetingActivity" />
        <activity android:name=".Teacher.AddBatch" />
        <activity android:name=".CourseContent" />
        <activity android:name=".Teacher.CreateCourse" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".profileActivity" />
    </application>

</manifest>